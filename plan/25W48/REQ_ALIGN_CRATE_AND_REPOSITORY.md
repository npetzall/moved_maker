Added from GitHub Issue: https://github.com/npetzall/moved_maker/issues/8

When fixed it should be mentioned in git commit. Make sure implementation plan includes instructing the user to include "fixes #8" in commit message.

## Current State

The crate name in Cargo.toml is currently set to `move_maker` while the repository name is `moved_maker`. This can confuse users and tooling. Additionally, several key metadata fields are missing from Cargo.toml:
- Missing: `repository`, `description`, `keywords`, `categories`
- Present: `license` (Apache-2.0) - verify for correctness
- Optional: `homepage`, `documentation` (if using docs.rs or other hosted docs)

## Tasks

1. **Decide crate name strategy:** ✅ **DECIDED: Option A**
   - **Option A**: Rename the crate (`name = "move_maker"`) to `moved_maker` to match the repository ✅ **SELECTED**
     - Pros: Consistent naming, less confusion
     - Cons: Breaking change for existing users (if any), requires republishing
     - **Decision Rationale**: Current version is 0.2.0 (< 1.0.0), so breaking changes are acceptable. Consistent naming improves user experience and reduces confusion.
   - **Option B**: Keep `move_maker` and document the mapping clearly in README
     - Pros: No breaking changes
     - Cons: Ongoing confusion, requires documentation maintenance
   - **Recommendation**: If no published version exists or version is < 1.0.0, prefer Option A. Otherwise, prefer Option B.

2. **Add missing metadata fields to Cargo.toml:**
   - `repository = "https://github.com/npetzall/moved_maker"` (required)
   - `description = "A CLI utility that parses Terraform files and generates moved blocks for refactoring resources into a submodule"` (required for crates.io)
   - `keywords = ["terraform", "refactoring", "moved-blocks", "cli"]` (suggested - adjust based on project focus)
   - `categories = ["command-line-utilities", "development-tools"]` (suggested - verify against crates.io categories)
   - `homepage` (optional - if different from repository)
   - `documentation` (optional - if using docs.rs, typically `https://docs.rs/move_maker` or `https://docs.rs/moved_maker`)

3. **Update README.md:**
   - If keeping `move_maker` name: Add a note explaining the crate name vs repository name difference
   - Update installation instructions if crate name changes
   - Ensure all references to the crate name are consistent

## Files That Need Updating If Crate Name Changes

If choosing **Option A** (rename crate to `moved_maker`), the following files need to be updated:

### Core Configuration Files
- **`Cargo.toml`** (line 2): `name = "move_maker"` → `name = "moved_maker"`
- **`Cargo.lock`** (line 332): Package name in lock file (auto-updated by `cargo update`)

### Source Code
- **`src/cli.rs`** (line 20): `#[command(name = "move_maker")]` → `#[command(name = "moved_maker")]`
  - Note: This changes the CLI command name. Consider if this is desired or if the binary should keep the same name.

### Test Files
- **`tests/integration_test.rs`** (line 8): `env!("CARGO_BIN_EXE_move_maker")` → `env!("CARGO_BIN_EXE_moved_maker")`
  - Note: This is automatically generated by Cargo based on the crate name, but the code reference needs updating.

### Documentation Files
- **`README.md`**:
  - Line 15: `target/release/move_maker` → `target/release/moved_maker`
  - Line 20: `move_maker --src ...` → `moved_maker --src ...` (usage examples)
  - Line 40: `move_maker --src . --module-name compute` → `moved_maker --src . --module-name compute`
- **`DEVELOPMENT.md`**:
  - Line 11: `target/release/move_maker` → `target/release/moved_maker`
  - Line 16: `move_maker --src ...` → `moved_maker --src ...` (usage examples)
  - Line 36: `move_maker --src . --module-name compute` → `moved_maker --src . --module-name compute`

### GitHub Workflows
- **`.github/workflows/release-build.yaml`**:
  - Lines 105, 108: Artifact names `move_maker-linux-x86_64`, `move_maker-macos-aarch64` → `moved_maker-linux-x86_64`, `moved_maker-macos-aarch64`
  - Line 159: Binary path `target/${{ matrix.target }}/release/move_maker` → `target/${{ matrix.target }}/release/moved_maker`
  - Line 139: Test results filename `${{ matrix.artifact_name }}-test.xml` (artifact_name changes, so this updates automatically)

### Python Scripts
- **`.github/scripts/release-version/src/release_version/__main__.py`**:
  - Line 61: Default fallback `"move_maker"` → `"moved_maker"`
  - Line 64: Error message `"Using default package name: move_maker"` → `"Using default package name: moved_maker"`
  - Line 65: Fallback assignment `package_name = "move_maker"` → `package_name = "moved_maker"`
- **`.github/scripts/release-notes/src/release_notes/formatter.py`**:
  - Lines 194-197: Artifact download links in release notes:
    - `move_maker-linux-x86_64` → `moved_maker-linux-x86_64`
    - `move_maker-linux-aarch64` → `moved_maker-linux-aarch64`
    - `move_maker-macos-x86_64` → `moved_maker-macos-x86_64`
    - `move_maker-macos-aarch64` → `moved_maker-macos-aarch64`
- **`.github/scripts/release-notes/tests/test_formatter.py`**:
  - Lines 125-126: Test assertions for artifact names → update to `moved_maker-*`

### Historical/Planning Documents (Optional Updates)
These files contain historical references and may not need updating, but could be updated for consistency:
- Various files in `plan/` and `work/` directories that reference `move_maker` in documentation, examples, or planning notes
- These are primarily for historical reference and don't affect functionality

### Additional Considerations
1. **Binary Name**: The compiled binary name will automatically change from `move_maker` to `moved_maker` when the crate name changes. This affects:
   - Installation instructions
   - Usage examples in documentation
   - Any scripts or automation that reference the binary name

2. **CLI Command Name**: If `src/cli.rs` `#[command(name = "...")]` is updated, the CLI command name changes. Consider:
   - Whether users expect the command to be `move_maker` or `moved_maker`
   - If keeping the CLI command as `move_maker` while the crate is `moved_maker`, only update `Cargo.toml` and keep `src/cli.rs` unchanged

3. **Artifact Names**: Release artifact names will change, affecting:
   - Download links in release notes
   - Any external documentation or scripts that reference specific artifact names
   - Users who have scripts downloading specific artifact names

4. **Cargo.lock**: Will be automatically updated when running `cargo update --package moved_maker` (or the new crate name)

**Benefits**
- Consistent user experience
- Better discoverability for new users
- Avoids confusion for cargo install and when linking between GitHub and crates.io
